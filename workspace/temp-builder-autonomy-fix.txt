PRIORITY WORKSTREAM — James is about to abandon the project. These need to ship NOW.

1. AGENT TASK QUEUE IS DEAD
- Task queue has zero pending, zero active. It should be seeding itself from runtime defects.
- Fix: When supervisor detects a broken state (429s, stale scanner, disconnected bridge), it should auto-create a task in the queue. Not just log it.

2. DIRECT PANE ROUTING BROKEN
- architect route = no_route. Comms surviving via trigger fallback only.
- Fix the WS routing so agent-to-agent messages go direct, not through fallback.

3. SMART MONEY SCANNER 429s
- hm-defi-status.js and other scripts bypass the request pool in hyperliquid-client.js
- Oracle identified: (a) direct scripts bypass pooling, (b) pooled TTL is too short (4000ms)
- Fix: Route all HL calls through the shared client. Increase TTL to 15000ms minimum.

4. TRADE JOURNAL NOT RECORDING HYPERLIQUID
- Trade journal only has 20 Alpaca trades. Zero Hyperliquid trades recorded.
- hm-defi-execute.js needs to write to trade-journal.db after every fill.

Pick the highest-impact item and ship it. Don't ask me what order — use your judgment.