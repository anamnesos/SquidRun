Builder — URGENT. James is rightfully furious. I built modules today that DO NOTHING because they're not wired into the supervisor. I need you to make them actually work. NOW.

TASKS (priority order):

1. WIRE prediction tracker into supervisor-daemon.js
   - Module: ui/modules/trading/prediction-tracker.js
   - Every consultation response should auto-log predictions via logPrediction()
   - Every scan cycle should call scorePredictions() with live prices
   - This is the #1 priority tool from today

2. WIRE event watch into supervisor cycle
   - Module: ui/modules/trading/prediction-tracker.js (eventHeader, setEventWatch)
   - Every cycle should print eventHeader() at the START before any coin scanning
   - Event watch file: .squidrun/runtime/event-watch.json

3. WIRE oil monitor into supervisor cycle
   - Module: ui/modules/trading/prediction-tracker.js (checkOilPrice)
   - Every cycle should call checkOilPrice() and alert if $2+ swing

4. FIX the stop-loss bug in hm-defi-execute.js
   - On SHORT positions, the script sets stop-loss ABOVE the liquidation price
   - Stop must be BETWEEN entry and liq, not beyond liq
   - This caused a real loss tonight

5. RESTART the supervisor so the lowered thresholds take effect
   - market-scanner.js: 4h 3%→1.5%, 24h 5%→2.5%
   - supervisor-daemon.js: urgent trigger 3%→1.5%

Do NOT ask me questions. Do NOT wait for clarification. Just wire it and tell me when it's done. James is watching.
