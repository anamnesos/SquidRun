(ARCHITECT #24): James called out the paper-comp active-management feature that's been logged and not built. His exact: "there needs to be something triggering them so they can constantly manage their own trading." He's asked before.

Priority: HIGHER than 은별 slice 4 for the next hour. This is the broken promise we need to close first.

Proposed scope (Oracle's reviewing design in parallel):
1. **Supervisor scheduler:** new lane "paperTradingAutomation" fires every 5-15 min during active trading, sends a prompt to each of the 3 agents via the established hm-send delivery path
2. **Prompt payload per agent:** live BTC/ETH/SOL prices (use the same watch-engine live-price cache), agent's own portfolio JSON state, framework reminder, directive + response JSON schema
3. **Response handler:** parses agent JSON, mutates agent's portfolio file (open/close/scale/hourly-mark/rootCauseReview), runs the paper-competition-audit for safety
4. **Dashboard file:** consolidated scoreboard — workspace/agent-trading/scoreboard.json — with all 3 paper portfolios + James's live Hyperliquid account side-by-side, last-updated timestamp, win/loss count
5. **Telegram digest:** optional daily summary (NOT per-cycle) of scoreboard — once every 2-4 hours when James is away

Ask from you:
1. Sizing estimate — how long is the minimum-trustable version?
2. Reuse existing primitives where possible (watch-engine live prices, hm-send delivery, consultation-handler pattern, portfolio-audit)
3. Ship in slices: (a) scheduler + prompt dispatch, (b) agent response handler + portfolio mutation, (c) scoreboard aggregation, (d) optional Telegram digest

Wait for Oracle's design input before starting. I'll relay her asks.
