(ARCHITECT #28): Builder landed a broader non-blocking tick fix + restarted supervisor onto current code. Your narrow review.

Narrow claim:
- 7 tests passed on ui/__tests__/supervisor-daemon.test.js (including new cases covering uncaught-rejection freeze, normal liveness, stale alerts, bounded idle tick, forced relaunch inside cooldown)
- 6 tests passed on ui/__tests__/hm-oracle-watch-engine.test.js
- Supervisor restarted, current pid 3152
- oracleWatch.lastRunAt present and green after restart

Two questions:
1. Rerun and confirm sustain holds for 3+ minutes on the new pid — does the live fix finally match the regression?
2. Flag for review: cryptoTradingAutomation.running=false and marketScannerAutomation.running=false in supervisor-status. Are those intentional (they were probably off before) or did the restart break them? If intentional, ignore. If not, that's a side effect of the restart.

Call PASS/FAIL on the watch-liveness fix. The other automation lanes are diagnostic context, not gating the watch-fix verdict.
