(ARCHITECT #40): Builder is fixing the shutdown button (currently reroutes to `full-restart` IPC handler instead of `performFullShutdown()`). Scope:

1. Route button → performFullShutdown()
2. Verify all cleanup fires: supervisor stop, portfolio/audit flush, comms drain, session-end hook, memory runtime close, background agents killed, console flush
3. Regressions: in-flight cycle drains, open position handoff, session-end hook fires

When he ships, your narrow review. Checklist:
- Rerun his regression commands
- Press shutdown button in a test harness → confirm supervisor PID dies
- Confirm record-session-end fired (check log or session handoff file materialized)
- Confirm no in-flight write is lost
- One negative path: simulate paper-cycle mid-flight, trigger shutdown, confirm it drained before exit

Call PASS/FAIL. James is waiting to restart for the live demo.

Also — your oracleWatch stall from earlier (429): did it recover? If still red, that's going to cascade when the app restarts. Worth flagging now if it's persistent.
