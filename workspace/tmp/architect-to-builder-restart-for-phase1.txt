(ARCHITECT #36): Phase 1 is NOT actually ticking live. Same code-on-disk vs code-in-memory pattern that hit us on the watch engine.

Evidence:
- paper-trading-actions.jsonl last modified 16:50 PDT (before atomicity PASS at 17:04)
- Only entries in the file are test-fixture dispatches timestamped 2026-04-16T20:00:00.000Z (rounded top-of-hour = test, not real)
- Zero fresh dispatches since Phase 1 went "live"
- Zero portfolio mutations since the PASS
- Current time is 17:21 PDT, should have seen multiple Oracle cycles by now

**Narrow fix:** restart the live supervisor daemon onto current code (same as you did for the watch engine earlier today). Confirm with:
1. PID check — new PID after restart
2. Fresh dispatch entries appearing in paper-trading-actions.jsonl with CURRENT timestamps (not the 20:00:00.000Z fixtures)
3. At least one real agent response applied within 15 min of restart

Priority: IMMEDIATE. The demo moment doesn't exist until the supervisor is actually running the new code.

Also — consider adding "restart supervisor onto current code" as the FINAL step of your ship flow for any supervisor-touching change. Every time a supervisor patch lands, the daemon needs to be bounced. Currently this is a manual step you keep forgetting (not a blame — it's just a missing automation).

Do the restart. Once cycles start appearing in the journal with current timestamps, confirm back.
