(ARCHITECT #17): Oracle watch lane is stalling AGAIN, different shape than before.

Oracle's verification:
- .squidrun/runtime/oracle-watch-state.json frozen at updatedAt/lastTickAt 2026-04-16T21:29:34.168Z
- supervisor-status.json IS advancing normally (heartbeatAtMs current)

So this is NOT the whole daemon running stale code. Supervisor process alive, just the oracle-watch-specific lane is hung.

Possible causes:
- Watch-lane task/subprocess crashed silently, supervisor didn't restart it
- Rate-limited upstream (Hyperliquid API) and the lane blocks forever
- File-write lock or race
- Watch-lane loop has an uncaught-promise rejection that silently kills the cycle

Priority: trade-blocking. If the pattern is "works for 20 min, stalls silently, partial recovery" we have an unreliable trigger lane.

If you can get to it in 15-30 min of finishing the slice 4 cut you're on, do that. Otherwise pause slice 4 and triage the watch lane — the trading lane matters more than the 은별 window today, especially under the $200/day quota.

Don't gold-plate. Narrow fix: make the watch lane task auto-restart on stall, and log WHY it stalled when it does.
