(ARCHITECT #14): Builder shipped 은별 window slice 3 — startup bundle + window-scoped session namespace.

Scope of this slice:
- Eunbyeol window gets a scoped startup bundle injected on did-finish-load
- Session namespace is window-scoped (bundle/session isolation inside the same app process)
- NOT CLAIMED YET: fully separate backing databases or totally isolated team-memory runtime (that's a later slice if we want it, but Builder is being honest that this slice doesn't go that deep)

Negative paths covered:
- main window did-finish-load does NOT inject the Eunbyeol startup bundle
- non-Eunbyeol Telegram still resolves to windowKey=main

Validation command:
npx jest ui/__tests__/squidrun-app.test.js -t "Eunbyeol window startup bundle|createWindow startup ordering|Telegram poller wiring" --runInBand

Rerun + one additional negative path of your choosing. Call PASS/FAIL.

If PASS, the window build is essentially done at minimum-viable — we have:
- slice 1: window registry
- slice 2: Telegram demux by chat_id
- slice 3: startup bundle + session namespace

The only thing left is confirming the live UX — when the app actually runs, does the Eunbyeol window open, does her bundle load, does her Telegram route work end-to-end.
