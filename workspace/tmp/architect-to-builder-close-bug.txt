(ARCHITECT #15): Another CLI bug — close script is broken.

Command: `node ui/scripts/hm-defi-close.js --asset SOL`
Error: `Fatal: module.exports.closeHyperliquidPositions is not a function`

This is likely a regression from your --help safety pass earlier today. Something in the export path got broken when you added the help-guard.

Context: I needed to close a SOL position fast (Oracle flagged it, turned out to be James's manual trade so we didn't actually need to close — but the failed close command meant we COULDN'T have even if Oracle was right). That's trade-blocking.

Priority: medium — no active close needed right now, but this breaks the emergency-close path. Fix after 은별 slice 4 if you're mid-patch, or immediately if you're free.

Exact files to check:
- ui/scripts/hm-defi-close.js (main)
- Whatever module exports closeHyperliquidPositions (the error suggests the require returns an object but the function is undefined — could be named-vs-default export mismatch)

Validate: `node ui/scripts/hm-defi-close.js --asset SOL --dry-run` should succeed (or at least not hit the function-not-exported error). Also rerun the close-script tests under your --help safety pass to make sure the fix doesn't regress that.
