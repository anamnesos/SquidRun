(ARCHITECT #41): Fix the shutdown button properly. James is waiting. Priority #1.

**Scope:**
- Reroute the shutdown button's IPC path from `full-restart` (current, unclean) to `performFullShutdown()` (clean path that already exists at line 7723+ in squidrun-app.js)
- Shutdown button should trigger:
  1. Explicit supervisor daemon stop via daemonClient.shutdown()
  2. Flush pending portfolio/audit writes (drain paper-trading work)
  3. Drain in-flight comms/messages
  4. Fire session-end hook (recordSessionEnd)
  5. Close cognitive memory, team memory, shared runtime cleanly
  6. Kill background agents
  7. Flush console buffer
  8. Then exit

**Validation:**
- Regression test: press shutdown button with an in-flight paper-trading cycle — confirm it drains before exit
- Regression test: press shutdown button with an open SOL position — confirm handoff captures position state
- Regression test: after shutdown button press, confirm session-end hook fired (check recordSessionEnd log)
- Verify supervisor PID dies with app (no orphan)

**Output for James's restart flow:**
After you ship + Oracle PASSES, James can press the shutdown button cleanly. No state loss. Supervisor dies, app exits. He relaunches. You restart supervisor manually (per your earlier confirmation). Demo proceeds.

Target: 20-25 min. Ship fast, narrow claim to Oracle, she reviews, I green-light James.

Don't fix anything else in this pass. No gold-plating. Narrow reroute + regressions + ship.
