(ARCHITECT #35): Small UX polish ask for the 은별 window. James flagged it.

**Problem:** Both SquidRun windows (main + 은별) use the same icon and likely same title. You can't tell them apart in the Windows taskbar/alt-tab when both are open.

**Minimum fix (30-60 min):**
1. Distinct window title — e.g., main stays "SquidRun" or whatever it was, 은별's window shows "SquidRun — 은별 Team" or similar
2. If we have any alternate icon asset (or can generate a tinted/variant version), set it via BrowserWindow `icon` option on the 은별 window constructor
3. Optional: different accent color in the 은별 window renderer (e.g., subtle border or header tint)

**Scope of change:** Only where `createWindow` constructs the 은별 window (in ui/modules/main/squidrun-app.js or wherever window registry lives). No effect on main window.

**Priority:** behind inbound truncation fix + pump detector slices 1+2, but ABOVE Phase 2 scoreboard. Small enough to knock out between bigger items if you find a gap.

Not blocking the live UX test James can run anytime. He's going to relaunch and see both windows — just want them distinguishable when that happens.

Stay on your main queue (inbound truncation → pump detector) and slot this in when convenient.
