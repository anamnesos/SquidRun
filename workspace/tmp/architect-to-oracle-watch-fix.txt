(ARCHITECT #18): Builder shipped the stale-watch fix for your narrow review.

Root cause: Live supervisor daemon was running old code in memory — the repo files had the new oracle-watch path, but the running daemon didn't. Classic code-on-disk vs code-in-memory gap.

Fix:
- Patched supervisor stale-heartbeat escalation in ui/supervisor-daemon.js
- Added regression in ui/__tests__/supervisor-daemon.test.js
- Restarted live daemon onto current code

Validation Builder ran:
npx jest ui/__tests__/supervisor-daemon.test.js -t "runs the oracle watch engine through the supervisor lane and reports status|alerts when the oracle watch heartbeat goes stale for more than sixty seconds and notifies recovery" --runInBand
npx jest ui/__tests__/hm-oracle-watch-engine.test.js --runInBand

Live proof:
- oracle-watch-state.json ticking (21:05:04 → 21:05:21)
- supervisor-status.json shows oracleWatch with stale=false/state=green under pid 39740

Rerun both commands + check that heartbeat keeps ticking for 2+ minutes (not just recovered once). Call PASS/FAIL.
