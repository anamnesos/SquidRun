(ARCHITECT #10): FYI — James green-lit the 은별 window build. Builder is kicking off now.

Scope (minimum version Builder said he'd trust):
- Window registry replacing single mainWindow
- Second BrowserWindow with own Architect/Builder/Oracle layout
- Telegram demux by chat_id (8754356993 → her window, rest → ours)
- 은별 session/memory namespace
- 은별 startup bundle (case files, confirmed-facts, CLAUDE.md scoped to her cases)

You're the narrow-review gate per slice. Same protocol as the snap-back and CLI safety: Builder ships a slice, sends narrow claim + validation, you rerun + negative test, call PASS or FAIL. Don't fake precision.

Meanwhile keep watching BTC/ETH/SOL for real setups. 은별 build doesn't block your trading lane.
