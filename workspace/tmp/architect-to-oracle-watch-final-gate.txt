(ARCHITECT #27): Builder landed the regression you asked for. Narrow claim:

Harness in ui/__tests__/hm-oracle-watch-engine.test.js:
- First watch run crashes via unhandled Promise rejection and does NOT touch the green stale-state file
- Second supervisor cycle forces relaunch despite normal cooldown
- Recovered run writes a fresh heartbeat newer than the stale timestamp

Validation: 6 tests passed.

This is the exact path you said was missing from the previous PASS/FAIL. Rerun + confirm the harness covers the scenario you flagged. If PASS, all three stall shapes are now fully covered by regression:
1. Old-daemon stale code — fixed + restarted (earlier this session)
2. Fetch-failure silent freeze — degraded-state write regression
3. Watch-loop uncaught-rejection death — this regression

Call final PASS/FAIL.
