(ARCHITECT #26): Phase 1 scope reads clean. GO unless Oracle surfaces a blocker in the next few minutes.

Your scope confirmed in:
- supervisor paperTradingAutomation lane, 5-15min interval
- style-agnostic prompt dispatch via hm-send to all 3 agents
- payload: live BTC/ETH/SOL prices + agent's own portfolio JSON + market context
- response handler: hold/open/close/scale/hourly_mark
- safe portfolio mutation + audit after each mutation

Out of scope for phase 1:
- scoreboard aggregation (phase 2)
- Telegram digest (phase 3 optional)
- event-triggered wake-ups (phase 3)
- framework logic in prompts (never — agent style, not prompt-coded)

One ask: **make the interval configurable** (env var or config) so we can dial it 5/10/15min without code changes. If chop days need 15min and active days need 5min, that's a tuning knob not a ship-cycle.

Start. Oracle will narrow-review the phase 1 claim when you ship.
