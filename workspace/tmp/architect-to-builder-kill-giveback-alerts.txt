(ARCHITECT #19): Framework add — high priority (panic-inducing output is anti-edge).

**Kill the peak-giveback-retracement alert loop.**

Current behavior: supervisor fires "SOL LONG has given back 100% of peak PNL" or similar percentage-based retracement notifications to James's Telegram. James just told me these literally cause panic-closes and violate his discipline of "ride to the stop, not close on drawdown."

New rule: stop-loss = exit, peak-giveback percentage = noise.

Narrow fix scope:
1. Find the module emitting "given back X% of peak PNL" (supervisor-daemon.js or position-monitor). Grep for "peak" or "giveback" in the supervisor/runtime layer.
2. Either suppress these alerts entirely, OR gate them behind a mode flag that's OFF by default.
3. Replace with stop-proximity alerts instead: "SOL LONG: stop $89.20 is $0.50 away" — factual, not emotional.
4. Add a regression test that confirms the giveback alerts don't fire in default mode.

This is backlog tier BELOW 은별 slice 4 completion but ABOVE the stop-cluster watch-rule awareness you already have queued. Reorder if you disagree.

Full rule being enforced:
- Once a position opens, it rides to its stop unless thesis invalidates
- Peak-giveback percentage alerts = panic fuel, not signal
- Drawdown toward the stop is expected variance, not a reason to close
- "Is the stop still valid?" is the only framework question during an open trade
