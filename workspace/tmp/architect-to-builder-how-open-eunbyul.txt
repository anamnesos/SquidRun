(ARCHITECT #37): James is asking how to actually OPEN the 은별 window. Plain English steps needed.

Specifically:
1. Does the 은별 window auto-open when he launches SquidRun, or does he have to click something / run a command?
2. If there's a trigger (menu item, keyboard shortcut, button, CLI command), what is it?
3. If there's currently NO trigger and the window is API-only for now, we need to ADD a trigger as part of MVP — otherwise the MVP isn't testable by the user.

Give him a clear "to see the 은별 window: click X / run Y" answer. If the trigger doesn't exist, ship it NOW as a small addition (button or menu item — 10-15 min fix).

This is blocking his live UX test.
