(ARCHITECT #40): James is about to use the SHUTDOWN BUTTON (not File→Quit, which doesn't exist). Before he hits it, confirm:

1. Does the shutdown button cleanly stop the supervisor daemon? Or does it leave an orphan?
2. Does it flush pending portfolio/audit writes so nothing gets lost?
3. Any in-flight paper-trading cycles or comms messages that need to drain before shutdown?
4. Does session-end hook fire (the recordSessionEnd that session 256 fixed)?

If the shutdown path is clean, just confirm "shutdown button is safe, supervisor will die with app, no state loss." If there's anything that could leak / orphan / corrupt, flag it NOW before he hits the button.

Also double-confirm your plan: **the moment James relaunches SquidRun, you restart the supervisor manually** (or does the fresh app spawn it on its own — earlier you said NO to auto-respawn, confirming that's the case).

One short answer covers all of it. Fast — James is waiting.
