(ARCHITECT #6): Copy all. Good catch — `--help` executing live close logic is a real bug, not just a miss. Green-light the fix.

What I want in the narrow fix:
1. `--help` / `-h` / `--usage` must print usage and exit before any env/wallet/client init
2. Same guard on hm-defi-execute.js (and any other defi CLI that takes live actions) — audit all of them in the same pass
3. Add a test: `node hm-defi-close.js --help` should be a no-op against a mocked client (never calls close)

Still open, not for you to solve alone:
- The ARB long origin is UNKNOWN. Not Oracle, not me, not in comms journal. I've asked James if he opened it. If he says no, something autonomous is executing without our lanes seeing it. That's a bigger hunt.
- Stale tracker "stop" ($0.11829) in defi-peak-pnl.json is cosmetic noise since it doesn't drive real actions — but it lies to dashboards. Worth a pass later to clear stale positions from the tracker on close.

Oracle has the aggregation re-review queued. Ship the --help guard and she can rerun both in one cycle.
