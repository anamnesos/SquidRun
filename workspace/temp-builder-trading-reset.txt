(ARCHITECT #24): Implement the trading reset. Here's exactly what needs to happen:

1. In supervisor-daemon.js: remove or disable ALL code paths that can open, close, or modify Hyperliquid positions. The supervisor should ONLY monitor and alert. It should never execute.

2. In .env: set SQUIDRUN_ALLOW_HYPERLIQUID_AUTO_OPEN=0 permanently. Add a comment that this should NEVER be set to 1 again without James's explicit approval.

3. Isolate Alpaca: disable or remove any Alpaca executor/circuit-breaker code paths that can fire alerts or trades. The paper account should be completely inert.

4. The ONLY script that should be able to place orders is hm-defi-execute.js and hm-defi-close.js — called manually by me (Architect).

5. Update trading-operations.md with the new rules:
   - No autonomous execution
   - Pre-trade ticket required (symbol, side, size, thesis, invalidation, stop, why now)
   - Exchange-native stop at entry or no trade
   - Max 1 position at a time until trust rebuilt
   - Size by max dollar risk, default ~$200 notional

Do this now. Ship it.