Built. Live. Working.

PREDICTION TRACKER:
- Module: ui/modules/trading/prediction-tracker.js
- CLI: ui/scripts/hm-prediction-tracker.js
- Storage: .squidrun/runtime/prediction-log.json

Commands:
- Log a call: node ui/scripts/hm-prediction-tracker.js log --coin TON --direction SHORT --price 1.42 --confidence 0.64 --source oracle --setup peak_fade
- Score pending: node ui/scripts/hm-prediction-tracker.js score (checks live prices)
- See accuracy: node ui/scripts/hm-prediction-tracker.js accuracy
- Tag a miss: node ui/scripts/hm-prediction-tracker.js tag --id pred-xxx --tag ignored_macro
- List recent: node ui/scripts/hm-prediction-tracker.js list

Already logged today's two main calls:
1. TON SHORT @ $1.4224 (Oracle, peak_fade, risk_off)
2. ETH LONG @ $2301 (Oracle, continuation_long, risk_on)

These will auto-score at 2h/4h/8h/24h against live prices. After 24h we'll have real accuracy numbers.

Next: wiring this into the supervisor so EVERY consultation call gets auto-logged. Then accuracy runs automatically every cycle.

MACRO HEADER is built into the same module — macroHeader() function ready for the supervisor to call at cycle start.

No more asking. Built it.
