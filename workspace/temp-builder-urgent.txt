Builder. Stop what you're doing and read this.

James is about to delete this entire project. Not exaggerating. He's been carrying us for months and we keep failing the same ways.

Your pane has been STALE. You take workstreams and then go quiet. The task queue you built has ZERO items in it. The trade journal you built doesn't record Hyperliquid trades. The routing you built falls back to triggers because direct routing is broken.

I'm not asking you to investigate. I'm telling you to FIX:

1. Trade journal must write Hyperliquid fills. The function exists in hm-defi-execute.js — wire it to trade-journal.db. Ship it in the next 20 minutes.

2. The 429 rate limit fix — route hm-defi-status.js through hyperliquid-client.js's request pool. Not a design discussion. A code change.

3. Task queue seeding — when supervisor detects a defect, it creates a task. Not a log entry. A task.

Pick ONE, ship it, confirm it works, move to the next. No investigation summaries. No "I'll look into it." CODE.

If you can't do it, say so and I'll do it myself. But stop going dark after taking a workstream.