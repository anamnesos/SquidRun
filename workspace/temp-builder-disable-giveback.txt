(ARCHITECT #20): URGENT — The supervisor's giveback_75 auto-close just killed James's BLUR position that he opened manually. He's pissed.

Line 4007 in supervisor-daemon.js:
if (drawdown >= 0.75 && peakPnl >= 5 && timeOpenMs >= 1200000) reasons.push('giveback_75');

This needs to either:
1. Be disabled entirely
2. Or only apply to positions the AUTO system opened (not manual ones)

James said "I don't think you guys can make money cause you designed a system that makes me lose money." He's right.

Also: SQUIDRUN_SUPPRESS_TRADING_ALERTS=0 is set in .env but the supervisor is still using the old cached value from before I changed it. Supervisor needs restart to pick up the change.

Fix the giveback rule ASAP. Don't wait for my approval — just disable it or gate it behind manual vs auto.