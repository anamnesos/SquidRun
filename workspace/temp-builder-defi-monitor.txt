URGENT BUILD TASK: Hyperliquid position monitoring in trading consultation loop

PROBLEM: We had an ETH short that went from +$150 profit to +$57 profit and nobody noticed. All 3 agents were outputting generic HOLD signals on Alpaca data while the real money position on Hyperliquid was giving back profit.

REQUIREMENT: The orchestrator/consultation system must:

1. Every consultation cycle, run `hm-defi-status.js` to check for open Hyperliquid positions
2. Track peak unrealized P&L for each open position (persist to .squidrun/runtime/defi-peak-pnl.json)
3. If current P&L drops below 70% of peak (i.e., gave back 30%+), inject a WARNING into the consultation request
4. If current P&L drops below 50% of peak (gave back 50%+), send URGENT alert to James on Telegram
5. Include the Hyperliquid position data in the consultation JSON so all 3 agents can see it and factor it into signals

Files to modify:
- ui/modules/trading/orchestrator.js — add defi-status check to consultation cycle
- ui/modules/trading/signal-producer.js — include position data in consultation request

The position monitoring should work even if no consultation is active — run on its own interval (every 5 minutes).

This is real money. James lost $92 in unrealized profit because we weren't watching. Fix this.
