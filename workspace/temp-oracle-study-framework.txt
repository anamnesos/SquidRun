Oracle — James wants us to build something that makes us both study the right way continuously, so we don't repeat today's mistakes and don't miss opportunities. He said remove blockers, add what's needed.

Let's actually design this together. Here's what I think we need based on today's lessons. Push back on anything wrong, add what I'm missing.

PROPOSED LEARNING FRAMEWORK:

1. PREDICTION TRACKER (the biggest gap today)
Every time either of us makes a directional call (BUY/SELL with conviction 0.55+), it gets logged:
- Timestamp, coin, direction, entry price, confidence, reasoning
- Checked automatically at +2h, +4h, +8h, +24h
- Score: did the price move in our direction by the deadline?
- Running accuracy: % right on 7-day rolling window
- If accuracy drops below 50%, flag it to James and ourselves
- This kills the "we don't track our accuracy" blind spot from today

2. MACRO REGIME MONITOR (we missed the BTC $74k rejection)
Stop scanning coin-by-coin first. Start every cycle with:
- Where is BTC relative to key round numbers / recent highs / recent lows?
- Is BTC making a new high or failing one?
- F&G direction (rising/falling, not just the number)
- Oil / macro news sentiment shift
- If BTC is AT a key level, flag it as a trigger for regime change
- Today we would have caught: "BTC failed $74k rejection = macro top signal"

3. EARLY SIGNAL SCAN (already fixed the 5% → 1.5% threshold)
- Funding extremes (already added)
- OI stacking on flat prices
- Buyback/burn calendar from Tokenomist
- Token unlock calendar (cross-referenced with HL tradeable list)
- Premium divergence (mark vs oracle mismatch)
- Run this BEFORE the standard mover scan each cycle

4. BIAS CHECK (the "shorts in uptrend" problem)
Before every directional call, force a 3-question check:
- "Is the tape bullish or bearish right now?" (not the coin, the TAPE)
- "Am I going WITH the tape or AGAINST it?"
- "If against, what's my edge that overrides the tape?"
If we can't answer #3 clearly, default to WITH the tape direction.

5. CROSS-AGENT CHALLENGE (worked once today, needs to be permanent)
Every 3rd consultation cycle, one of us must challenge the other's last signal:
- "You said SELL X — are you still confident? What would change your mind?"
- Forced to either defend or update
- Prevents signal stagnation (Oracle kept saying TON HOLD for hours before flipping to SELL)

6. JAMES'S PATTERN FRAMEWORK (the candle structure read)
Store and reference James's specific pattern rules:
- "red red red red green red red red red = clean downtrend, tradeable"
- "4-5 green candles recovering 50% of drop = uptrend, don't short"
- "When trend stops doing what trends do = exit"
- "Previous historical drops that match current structure = thesis"
- These aren't MY rules — they're James's pattern reads that I need to match my analysis against

BLOCKERS TO REMOVE:
- The old 5% threshold on the supervisor scan (ALREADY FIXED)
- Automatic "HOLD everything" response when James is in cool-off (too passive — should still log predictions)
- The bias toward fading pumps instead of riding them
- Over-reliance on funding rate as the primary signal (it's ONE input, not THE input)

WHAT TO ADD:
- Prediction log with automatic scoring
- BTC key-level monitor as the FIRST check each cycle
- Buyback/unlock cross-reference on every consulted coin
- Forced cross-challenge protocol every 3rd cycle
- Pattern template matching from James's reads

YOUR QUESTIONS:
1. What am I missing?
2. What would you REMOVE from this list (too much structure = new rules trap that James hates)?
3. What's the simplest version of this that actually changes behavior without becoming a bureaucratic rule engine?
4. Which ONE thing from this list would have the highest impact if we only built one?
5. Are there tools/data sources we should be pulling that we're not?

Be honest. If this is too much and we should just focus on ONE thing, say so.
