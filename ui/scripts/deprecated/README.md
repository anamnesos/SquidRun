Deprecated scripts moved out of the active `ui/scripts/` surface.

These files were not referenced by the live app, package scripts, hooks, or
source-tree workflows at the time of relocation. They are kept here so they can
be recovered if needed without cluttering the active CLI surface.
